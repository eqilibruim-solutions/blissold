<?xml version="1.0" encoding="UTF-8"?>
<odoo>

<!-- 	<record model="ir.ui.view" id="view_payment_transaction_form_inherit">
	    <field name="name">payment.transaction.form.inherit</field>
	    <field name="model">payment.transaction</field>
	    <field name="type">form</field>
	    <field name="inherit_id" ref="payment.transaction_form" />
	    <field name="arch" type="xml">
	        <xpath expr="//field[@name='state']" position="before">
	            <button type="object" name="action_force_done" states="pending" string="Validate" confirm="Are you sure you want to mark transaction as done? This action can't be undone."/>
	        </xpath>
	    </field>
	</record> -->

        <record id="payment.action_payment_transaction" model="ir.actions.act_window">
            <field name="name">Payment Transactions</field>
            <field name="res_model">payment.transaction</field>
            <field name='view_mode'>tree,kanban,pivot,form</field>
        </record>


</odoo>